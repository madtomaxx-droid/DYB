(() => { const key = 'dyb-cookie-preferences'; const banner = document.querySelector('.cookie-banner'); if (!banner) return; try { if (localStorage.getItem(key)) return; } catch (_) {} banner.hidden = false; banner.querySelector('[data-cookie-save]')?.addEventListener('click', () => { const preferences = { necessary: true, analytics: Boolean(banner.querySelector('[data-cookie="analytics"]')?.checked), marketing: Boolean(banner.querySelector('[data-cookie="marketing"]')?.checked), savedAt: new Date().toISOString() }; try { localStorage.setItem(key, JSON.stringify(preferences)); } catch (_) {} banner.hidden = true; }); })();
